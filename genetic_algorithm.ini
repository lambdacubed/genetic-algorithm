Initialization variables for the genetic algorithm (note: the lines must be double spaced, only have spaces (don't use tabs), and have a information line at the beginning)
num_init_parents = 1        # number of parents to start with

num_init_children = 10     # number of starting children
    
init_voltage = 40           # initial voltage on mirror actuators

filename = None             # name of file to read from

num_parents = 10            # number of parents in loop iterations

num_children = 100          # number of children in loop iterations

mutation_percentage = 20    # if you want 20% mutation, enter 20

data_acquisition_device = IC    # The options are "Andor", "NI_DAQ", "Picoscope", "IC", or "Test". Test doesn't acquire any data, it is used for software debugging.

mirror_communication_device = PCI    # The options are "PCI", "USB", or "Test". Test doesn't send any voltages it is used for software debugging.

<<<<<<< HEAD
fom_num = 1    # this denotes which figure of merit calculation you will use in figure_of_merit_functions.py

deformable_mirror = 37_square_1    # The options are "37_square_1". The format is "[number of actuators]_[arrangement of actuators]_[integer denoting the specific mirror]"
=======
fom_num = 3    # this denotes which figure of merit calculation you will use in figure_of_merit_functions.py
>>>>>>> lab_computer
